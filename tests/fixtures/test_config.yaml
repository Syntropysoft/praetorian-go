# Test Configuration
version: "1.0"
app:
  name: "test-app"
  version: "1.0.0"
  environment: "test"
database:
  host: "localhost"
  port: 5432
  name: "test_db"
logging:
  level: "info"
  format: "json"
